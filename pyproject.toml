[project]
name = "osumapscout"
version = "0.1.0"
description = "An osu!standard map recommendation application"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "httpx",
    "uvicorn",
]
